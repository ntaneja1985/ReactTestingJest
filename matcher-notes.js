[{"content":"function toContainRole(container,role,quantity = 1) {\r\n    //query to find elements with the specified role inside the container\r\n      const elements = within(container).queryAllByRole(role);\r\n      \r\n    //return the object\r\n    if(elements.length === quantity)\r\n      {\r\n        return {\r\n          pass:true\r\n        };\r\n      }\r\n\r\n    //return {\r\n      //pass:true -->everything is good, test passes\r\n    //}\r\n\r\n    //if the test fails, then pass a message also why it failed\r\n    return {\r\n      pass: false,\r\n      message: () => `Expected to find ${quantity} ${role} elements. Found ${elements.length} instead`\r\n    }\r\n}\r\n\r\n//Register the custom matcher with expect\r\nexpect.extend({toContainRole})","type":"code","id":"4kezk"},{"content":"function FormData() {\r\n  return (\r\n    <div>\r\n    <button>Go Back</button>\r\n    <form aria-label=\"form\">\r\n    <button>Save</button>\r\n    <button>Cancel</button>\r\n    </form>\r\n    </div>\r\n  )\r\n}\r\n\r\nrender(<FormData/>)","type":"code","id":"lnbwz"},{"content":"test('the form displays 2 buttons',()=>{\r\n  render(<FormData/>)\r\n\r\n  const form = screen.getByRole('form');\r\n  const buttons = within(form).getAllByRole('button');\r\n\r\n  expect(buttons).toHaveLength(2);\r\n})","type":"code","id":"gkvj8"},{"content":"test('the form displays 2 buttons',()=>{\r\n  render(<FormData/>)\r\n\r\n  const form = screen.getByRole('form');\r\n  //const buttons = within(form).getAllByRole('button');\r\n\r\n  //expect(buttons).toHaveLength(2);\r\n  expect(form).toContainRole('link',10)\r\n})","type":"code","id":"wrbmh"}]